#include "main.h"
/**
  * mul - fucntion that prints two integers
  * @a: first input
  * @b: second input
  * Return: result if successful
  */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
