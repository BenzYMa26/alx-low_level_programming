#include <stdio.h>
/**
 * main - a program that prints the alphabet
 *in lowercase, followed by a new line
 * Return: 0 when run properly and non zero if otherwise
 */
int main(void)
{
char ab;
for (ab = 'a'; ab <= 'z'; ch++)
{
putchar(ab);
}
putchar('\n');
return (0);
}
